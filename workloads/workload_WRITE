# insert only

recordcount=1000
operationcount=1000
workload=core

insertorder=ordered

readallfields=true

readproportion=0
updateproportion=0
scanproportion=0
insertproportion=1

requestdistribution=sequential

# 是否建立索引
# 如果这个值为true，则执行insert操作的时候，fieldlength的第一个byte为[0, 255]随机数字，后面的byte内容均相同；
hasIndex=true

# couchbase的内存使用
couchbase_memory=10240



